// FileCollector V3.0 "Phoenix"
// Centralized version constants - referenced by UI title, About box, reports.

namespace FileCollector.Core
{
    /// <summary>
    /// V3.0 version constants. Bump these and the whole UI/reports follow.
    /// </summary>
    public static class VersionInfo
    {
        public const string Version = "3.0.0";
        public const string CodeName = "Phoenix";
        public const string ProductName = "Сборщик файлов";
        public const string TitleBar = ProductName + " V" + Version + " (" + CodeName + ")";
        public const string ShortTitle = ProductName + " V" + Version;
        public const string Company = "POSTEX";
    }
}
